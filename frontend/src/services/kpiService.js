import apiService from './apiService'; // This path is likely correct if kpiService is in the same folder

class KpiService {
  // KPI Service - other methods can be added here if needed
}

export default new KpiService();
